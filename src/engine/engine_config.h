#ifndef _CHOCOLATE_KEEN_ENGINE_CONFIG_H_
#define _CHOCOLATE_KEEN_ENGINE_CONFIG_H_

#include <stdbool.h>

// Parses a NULL-terminated configuration line, without a trailing newline.
bool CVort_engine_parseConfigLine(const char *line);

#endif
